version: '3.7'

services:
  weather-bot:
    build:
      context: .
    volumes:
      - ./app:/app
    env_file:
      - .env
    ports:
    - 80:80
    restart: always

  redis:
    image: redis
    volumes:
      - weather-redis-data:/data
    restart: always

volumes:
  weather-redis-data: